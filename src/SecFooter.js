import React, { Component } from 'react';

class SecFooter extends Component{
    render(){
        return (<section className="SecFooter" id="SecFooter">
            <div>Footer</div>
        </section>);
    }
}

export default SecFooter;